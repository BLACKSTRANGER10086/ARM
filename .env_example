# 3-DOF 机械臂项目环境变量示例
#
# 使用方式：
# 1. 复制本文件并重命名为 .env
# 2. 填入你的 API Key 和模型配置
# 3. 重新启动 python .\web_ui.py 或命令行脚本
#
# 注意：
# - .env 里可以放真实密钥，.env_example 只放示例值。
# - 当前项目默认流程是：自然语言 -> LLM 输出 commands 语义 JSON -> 本地规划器生成轨迹。
# - LLM 不直接输出关节角或 move_joints，轨迹仍由本地 arm_planner.py 生成和校验。


# 必填：OpenAI 或 OpenAI 兼容接口的 API Key。
# 如果不填写，llm_planner.py 会无法调用 LLM，并回退到本地规则解析。
# 本地规则适合规范命令测试，但复杂自然语言理解能力不如 LLM。
OPENAI_API_KEY=sk-请替换为你的-api-key


# 可选：LLM 模型名。
# 不设置时，代码默认使用 llm_planner.py 里的 DEFAULT_MODEL。
# 当前代码默认值是 deepseek-v4-flash。
# 如果你使用官方 OpenAI，可以改成你账号可用的模型名。
OPENAI_MODEL=deepseek-v4-flash


# 必填：OpenAI 兼容接口地址。
# 本项目默认按第三方 API / 第三方网关 / 兼容 OpenAI 协议的模型服务来配置。
# 请填写服务商提供的 base_url，通常以 /v1 结尾。
#
# 示例：
# - https://你的网关地址/v1
# - https://api.deepseek.com/v1
# - http://127.0.0.1:8001/v1
OPENAI_BASE_URL=https://你的第三方-api-地址/v1


# 可选：是否优先跳过 LLM，直接用本地中文规则解析。
# 默认不要开启，正常使用建议保持注释或设为 0。
#
# 取值说明：
# - 0 / false / no / off / 留空：默认流程，优先调用 LLM 输出 commands 语义 JSON。
# - 1 / true / yes / on：调试流程，优先使用本地规则解析输入。
#
# 适合开启的场景：
# - 临时没有 API Key。
# - LLM 接口不可用。
# - 只想测试 arm_planner.py 的本地轨迹规划和 Web 3D 动画。
#
# 不建议开启的场景：
# - 希望理解模糊自然语言。
# - 希望支持更复杂的多段任务语义。
# - 希望测试 LLM 输出 commands JSON 的主流程。
# ARM_LOCAL_FIRST=0
